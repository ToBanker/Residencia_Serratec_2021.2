import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        height: 80,
        backgroundColor: 'red',
        // marginBottom: 20
    }
});

export default styles;